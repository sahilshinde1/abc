mydata=read.csv('F:/database/iris.csv')
mydata=mydata[0:50,1:4]
attach(mydata)
x<-cbind(sepal.length,sepal.width,petal.length,petal.width)
plot(mydata[,-5])
summary(x)

cor(x)

pca1=princomp(x,scores=TRUE,cor=TRUE)
summary(pca1)
str(pca1)

plot(pca1, type = 'l')
screeplot(pca1,type='line',main="Scree Plot")

biplot(pca1)

pca1$scores[pca1$scores[1:4,]

            
